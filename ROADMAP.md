# Roadmap

### GP Tracker (do later for personal use)

Using the RuneLite plugin **Persistent Bank**:

spending power = bank coins (id 995) + inventory coins (id 995) + grand exchange totalValueGp

- Ignore top-level `totalValueGp` — it counts all items owned, not spendable
- Display: spending power vs bond price → % progress toward bond
